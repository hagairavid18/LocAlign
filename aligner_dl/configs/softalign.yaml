trainer:
  exp_name: "baseline-2409-Softalign-sw-ligand_split"
  tags: ["baseline", "ligand_split"]
  max_epochs: 30
  validate_only: true
  profiler: false
model:
  name: Baseline
  args:
    model_name: "SoftAlignAligner"
 
dataset:
  validation:
    name: ScanNetDataset
    args:
      base_scannet_path: '/home/iscb/wolfson/hagairavid/scannet_outputs_1709'
      esm_model: "esm2_t30_150M_UR50D"
      df_path: datasets/csv_files/ligand_1708/val_baseline.csv
      seed: 81
      level: "atom"
      # n_samples: 100
      min_cath: 0
      max_cath: 4
 
dataloader:
  train_batch_size: 8
  valid_batch_size: 1
  test_batch_size: 1
  n_workers: 15